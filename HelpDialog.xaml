<Window x:Class="BpmMeasurer.HelpDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        lex:LocalizeDictionary.DesignCulture="zh-CN"
        lex:ResxLocalizationProvider.DefaultAssembly="Bpm Measurer"
        lex:ResxLocalizationProvider.DefaultDictionary="Langs"
        Title="{lex:Loc HelpDlg_Title}"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="#FF0D0D0D"
        MinWidth="420">
    <Window.Resources>
        <Style TargetType="Button" x:Key="CloseButtonStyle">
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="#FF4F46E5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="16,6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Help rows -->
        <ItemsControl Grid.Row="0" x:Name="HelpItemsControl">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Padding="0,8" BorderBrush="#FF2A2A2A" BorderThickness="0,0,0,1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="{Binding Operation}"
                                       Foreground="#FF818CF8"
                                       FontSize="13"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Center"
                                       Margin="0,0,16,0"
                                       TextWrapping="Wrap"/>
                            <TextBlock Grid.Column="1"
                                       Text="{Binding Description}"
                                       Foreground="#FFCCCCCC"
                                       FontSize="13"
                                       VerticalAlignment="Center"
                                       TextWrapping="Wrap"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Close button -->
        <Border Grid.Row="2" Margin="0,16,0,0">
            <Button Style="{StaticResource CloseButtonStyle}"
                    HorizontalAlignment="Center"
                    Click="CloseBtn_Click"
                    Content="{lex:Loc HelpDlg_Close}"/>
        </Border>
    </Grid>
</Window>
